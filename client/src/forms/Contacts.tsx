import React from "react";
import {connect} from "react-redux";

const Contacts = () => {
  return (
    <div>
      <h1>Contacts Page</h1>
    </div>
  )
}

export default connect(null, null)(Contacts)